# wb-mcp 渠道配置示例 —— 复制为 ~/.workbuddy/agents/<渠道名>.env 并填入真实值
# 渠道名（WB_PROFILE）只是文件名，权限由 WB_SCOPE 决定

# 在 liflow.cn/settings 自助签发的 API Key（勿提交真实 key）
WB_API_KEY=wbk_xxxxxxxxxxxxxxxx

# 权限档位：admin=全权 / readwrite=读写 / readonly=只读（只读模式 MCP 仅注册 wb_query）
WB_SCOPE=readonly
